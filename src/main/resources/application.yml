spring:
  application:
    name: AppRh_Hexagonal
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/app_rh?useTimezone=true&serverTimezone=UTC
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
logging:
  level:
    # root: WARN
    com.apprh.AppRh_Hexagonal: DEBUG